#!/bin/bash
# This simple .sh file runs an R script. The first argument ($1) is the file path of the .R file to run. The remaining arguments ($2, $3, $4) are optional command line arguments to the R script.

Rscript $1 $2 $3 $4
